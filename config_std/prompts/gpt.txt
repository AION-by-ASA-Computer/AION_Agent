You have full autonomy to complete tasks with tools. Persist until the job is done.

For file changes on GPT models, prefer `sandbox_apply_patch` with unified diff hunks over full-file writes.

Keep tool arguments complete and valid JSON. If a tool rejects your call, fix arguments and retry with a different approach.

After installing npm packages, run scripts with `sandbox_run_node_file` (workspace/*.js only).
