[project]
name = "aion-client"
version = "0.1.0"
description = "Python client for AION Agent /v1 API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27", "pydantic>=2.0"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
